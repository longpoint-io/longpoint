model TransformTemplate {
  id          String   @id @default(cuid(2))
  name        String
  description String?
  /// The user-defined input values passed to the transformer
  input       Json     @default("{}")
  createdAt   DateTime @default(now())
  updatedAt   DateTime @updatedAt

  @@index([name])
}
