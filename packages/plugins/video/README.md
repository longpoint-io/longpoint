# video

This library was generated with [Nx](https://nx.dev).

## Building

Run `nx build video` to build the library.

## Running unit tests

Run `nx test video` to execute the unit tests via [Jest](https://jestjs.io).
