# plugin-openai

This library was generated with [Nx](https://nx.dev).

## Building

Run `nx build plugin-openai` to build the library.

## Running unit tests

Run `nx test plugin-openai` to execute the unit tests via [Jest](https://jestjs.io).
